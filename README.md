# MMPlayerCore
vs2019竟然没法运行git保存的项目，所以再保留一版核心代码



2025/10/07

dllLib文件夹是用到的dll依赖，在生产release环境中通常放在.exe文件所在的目录，

在开发debug的过程中放在环境变量Path包含的路径中。



2025/09/14


把MMPlayer做成一个核心的模块，然后把这个核心模块编写成库，一个so也好，dll也好，静态库也好。
这个库是和平台无关的一个库，有了库之后，再到相应的平台上编写平台相关性的代码。
这样就可以做到，播放器核心逻辑和上层业务分离。